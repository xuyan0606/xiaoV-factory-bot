# 科为博钉钉机器人 — 配置指南

## 第一步：注册 ngrok（免费，1分钟）

1. 浏览器打开 https://dashboard.ngrok.com/signup
2. 可以用 GitHub/Google 账号登录，或邮箱注册
3. 登录后进入 https://dashboard.ngrok.com/get-started/your-authtoken
4. 复制你的 Authtoken（类似 `2h8F...xxxx`）

## 第二步：配置 ngrok

打开终端，运行：

```bash
~/.hermes/bin/ngrok config add-authtoken <你的token>
~/.hermes/bin/ngrok http 5001
```

你会看到类似：
```
Forwarding https://abc123.ngrok.io -> http://localhost:5001
```

记下这个 `https://abc123.ngrok.io` 地址

## 第三步：钉钉群里添加机器人

1. 打开钉钉 → 找到你的群
2. 群设置 → 智能群助手 → 添加机器人
3. 选择「自定义」(通过WebHook接入)
4. 机器人名称：科为博工厂助手
5. 勾选「outgoing( outgoing )」模式
6. POST地址填写：`https://你的ngrok地址/dingtalk/webhook?token=crvab_factory_2026`
7. Token填写：`crvab_factory_2026`
8. 勾上「安全设置」中的「加签」并填入同一token
9. 点完成

## 第四步：测试

在群里 @科为博工厂助手 发送：
```
帮助
```

应该就能收到回复了。

## 安全说明

- 所有请求都经过 Token 校验，错误 token 的请求会被拒绝
- 频率限制每秒最多10次请求
- ngrok 提供 HTTPS 加密传输
- 数据仅包含车间状态、设备清单、配方等生产信息
- 不包含财务、人事等敏感数据

## 本地文件

- 服务端：`/Users/xuyan/software/hermesWorkspace/factory_bot_server.py`
- 5个HTML页面也在同目录下

## 常驻运行

要让机器人7x24小时运行，需要把服务器一直开着：
- 你的 Mac 不能关机/休眠
- 或者部署到云服务器（推荐）
